import React from 'react';

// This component is currently not used in the application.
// It has been simplified to remove dependencies on removed types.
export const AllCategoriesPage: React.FC = () => {
    return (
        <div className="bg-white p-6">
            <h1 className="text-2xl font-bold text-center mb-6">All Categories</h1>
            <p className="text-center text-gray-500">This page is currently not in use.</p>
        </div>
    );
};
